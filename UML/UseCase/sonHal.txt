@startuml
left to right direction
skinparam packageStyle rectangle
actor hasta
actor doktor
actor yönetici
rectangle Hastane_Randevu_Sistemi{
  hasta -- (login)
  (login) .> (şifre kontrol) : include
  (Hata göster) .> (login) : extend
   hasta -- (randevu al)
  (randevu al) .>(maile bilgi gönder) : include
  hasta -- (randevu iptal et)
  (randevu iptal et) .> (maile bilgi gönder) :include
  hasta -- (randevu geçmişini görüntüle)
  hasta -- (hesap bilgilerini yönet)
  
  doktor -- (login)
  doktor -- (kendisine alınan randevuları görüntüle)
  doktor -- (randevu listesinde arama)
  doktor -- (hesap bilgilerini yönet)
  (randevu listesinde arama) .> (Hasta TC ile arama ) : include
  
  (login) -- yönetici
  (kullanıcıları görüntüle) --yönetici
  (kullanıcı sil) -- yönetici
  (kullanıcı sil) .> (maile bilgi gönder) : include
  (randevuları görüntüle)
  (randevu iptal et) -- yönetici
  (randevu iptal et) .> (maile bilgi gönder) : include  
  (hesap bilgilerini yönet) --yönetici
}
@enduml  
